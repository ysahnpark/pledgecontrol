<?php
/**
 * Models from schema: ecofy version 0.1
 * Code generated by TransformTask
 *
 */


/**
 * Controller class that provides web access to CustomField resource
 *
 * @todo: Add following line in app/routes.php
 * Route::resource('custom_field', 'CustomFieldController');
 */
class CustomFieldController extends \BaseController {

    // The service object
	protected $customFieldService;

	protected $layout = 'layouts.master';

	/**
	 * Constructor
	 */
	public function __construct() {
		parent::__construct();
		$this->addBreadcrumb([ Lang::get('document._name')]);
		$this->setContentTitle(Lang::get('document._name_plural'));
        //$this->customFieldService = new Service\CustomFieldService();
        $this->customFieldService = App::make('custom_field_service');
    }

	/**
	 * Display a listing of the resource.
	 *
	 * @return Response
	 */
	public function index()
	{
		$queryCtx = new \DocuFlow\Helper\DfQueryContext(true);
		$criteria = $queryCtx->buildCriteria();

		$records = $this->customFieldService->paginateCustomFields($criteria, $queryCtx->limit);
		$count = $this->customFieldService->countCustomFields($qparams);

        // $qparams is used by view to generate query string
		$qparams[self::PAGE_SIZE_PNAME] = $page_size;
		$this->layout->content = View::make('custom_field.index')
		    ->with('qparams', $queryCtx->qparams)
		    ->with('records', $records)
		    ->with('count', $count);
	}

	/**
	 * Show the form for creating a new resource.
	 *
	 * @return Response
	 */
	public function create()
	{
		$this->addBreadcrumb(['new']);
		$this->setContentTitle('New ' . Lang::get('custom_field._name') );

		$this->layout->content = View::make('custom_field.create');
	}

	/**
	 * Store a newly created resource in storage.
	 *
	 * @return Response
	 */
	public function store()
	{
		$data = Input::all();

		try {
            $record = $this->customFieldService->createCustomField($data);
            Session::flash('message', 'Successfully created!');

            return $this->redirectAfterPost(
            	array('save' => route('custom_fields.edit', array($record->sid))),
            	route('custom_fields.index')
            	);
        } catch (Services\ValidationException $ve) {
            return Redirect::to('custom_fields/create')
                ->withErrors($ve->getObject());
                //->withInput(Input::except('password'));
        } 
	}

	/**
	 * Display the specified resource.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function show($id)
	{
		$record = $this->customFieldService->findCustomField($id);

		$this->addBreadcrumb([$record->getName(), Request::url()]);
		$this->setContentTitle(Lang::get('custom_field._name') . ' - ' .  $record->getName());

		$this->layout->content = View::make('custom_field.show')
			->with('record', $record);
	}

	/**
	 * Show the form for editing the specified resource.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function edit($id)
	{
	    $record = $this->customFieldService->findCustomField($id);

		$this->addBreadcrumb([$record->getName(), Request::url()]);
		$this->setContentTitle(Lang::get('custom_field._name') . ' - ' .  $record->getName());

		$this->layout->content = View::make('custom_field.edit')
		    ->with('record', $record);
	}

	/**
	 * Update the specified resource in storage.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function update($id)
	{

		$data = Input::all();

        try {
            $record = $this->customFieldService->updateCustomField($id, $data);

            // @todo: Redirect to proper URL
            Session::flash('message', 'Successfully updated!');
            
            return $this->redirectAfterPost(
            	array('save' => route('custom_fields.edit', array($record->sid))),
            	route('custom_fields.index')
            	);
        } catch (Services\ValidationException $ve) {
            return Redirect::to('custom_fields/' . $id . '/edit')
                ->withErrors($ve->getObject());
                //->withInput(Input::except('password'));
        } 
	}

	/**
	 * Remove the specified resource from storage.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function destroy($id)
	{
		// delete
		$success = $this->customFieldService->destroyCustomField($id);

		if ($success) {
            Session::flash('message', 'Successfully deleted!');
            return Redirect::to('custom_fields');
        } else {
            Session::flash('message', 'Entry not found');
            return Redirect::to('custom_fields');
        }
	}
}
