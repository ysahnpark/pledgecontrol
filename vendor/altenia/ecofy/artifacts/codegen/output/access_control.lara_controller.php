<?php
/**
 * Models from schema: ecofy version 0.1
 * Code generated by TransformTask
 *
 */


/**
 * Controller class that provides web access to AccessControl resource
 *
 * @todo: Add following line in app/routes.php
 * Route::resource('access_control', 'AccessControlController');
 */
class AccessControlController extends \BaseController {

    // The service object
	protected $accessControlService;

	protected $layout = 'layouts.master';

	/**
	 * Constructor
	 */
	public function __construct() {
		parent::__construct();
		$this->addBreadcrumb([ Lang::get('document._name')]);
		$this->setContentTitle(Lang::get('document._name_plural'));
        //$this->accessControlService = new Service\AccessControlService();
        $this->accessControlService = App::make('access_control_service');
    }

	/**
	 * Display a listing of the resource.
	 *
	 * @return Response
	 */
	public function index()
	{
		$queryCtx = new \DocuFlow\Helper\DfQueryContext(true);
		$criteria = $queryCtx->buildCriteria();

		$records = $this->accessControlService->paginateAccessControls($criteria, $queryCtx->limit);
		$count = $this->accessControlService->countAccessControls($qparams);

        // $qparams is used by view to generate query string
		$qparams[self::PAGE_SIZE_PNAME] = $page_size;
		$this->layout->content = View::make('access_control.index')
		    ->with('qparams', $queryCtx->qparams)
		    ->with('records', $records)
		    ->with('count', $count);
	}

	/**
	 * Show the form for creating a new resource.
	 *
	 * @return Response
	 */
	public function create()
	{
		$this->addBreadcrumb(['new']);
		$this->setContentTitle('New ' . Lang::get('access_control._name') );

		$this->layout->content = View::make('access_control.create');
	}

	/**
	 * Store a newly created resource in storage.
	 *
	 * @return Response
	 */
	public function store()
	{
		$data = Input::all();

		try {
            $record = $this->accessControlService->createAccessControl($data);
            Session::flash('message', 'Successfully created!');

            return $this->redirectAfterPost(
            	array('save' => route('access_controls.edit', array($record->sid))),
            	route('access_controls.index')
            	);
        } catch (Services\ValidationException $ve) {
            return Redirect::to('access_controls/create')
                ->withErrors($ve->getObject());
                //->withInput(Input::except('password'));
        } 
	}

	/**
	 * Display the specified resource.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function show($id)
	{
		$record = $this->accessControlService->findAccessControl($id);

		$this->addBreadcrumb([$record->getName(), Request::url()]);
		$this->setContentTitle(Lang::get('access_control._name') . ' - ' .  $record->getName());

		$this->layout->content = View::make('access_control.show')
			->with('record', $record);
	}

	/**
	 * Show the form for editing the specified resource.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function edit($id)
	{
	    $record = $this->accessControlService->findAccessControl($id);

		$this->addBreadcrumb([$record->getName(), Request::url()]);
		$this->setContentTitle(Lang::get('access_control._name') . ' - ' .  $record->getName());

		$this->layout->content = View::make('access_control.edit')
		    ->with('record', $record);
	}

	/**
	 * Update the specified resource in storage.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function update($id)
	{

		$data = Input::all();

        try {
            $record = $this->accessControlService->updateAccessControl($id, $data);

            // @todo: Redirect to proper URL
            Session::flash('message', 'Successfully updated!');
            
            return $this->redirectAfterPost(
            	array('save' => route('access_controls.edit', array($record->sid))),
            	route('access_controls.index')
            	);
        } catch (Services\ValidationException $ve) {
            return Redirect::to('access_controls/' . $id . '/edit')
                ->withErrors($ve->getObject());
                //->withInput(Input::except('password'));
        } 
	}

	/**
	 * Remove the specified resource from storage.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function destroy($id)
	{
		// delete
		$success = $this->accessControlService->destroyAccessControl($id);

		if ($success) {
            Session::flash('message', 'Successfully deleted!');
            return Redirect::to('access_controls');
        } else {
            Session::flash('message', 'Entry not found');
            return Redirect::to('access_controls');
        }
	}
}
